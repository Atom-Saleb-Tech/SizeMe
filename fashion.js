function start() {
    var section = $("#currentSection").val();
    displaySection(sectionId);
}

function displaySection(sectionId) {
 /* for (var i = 0; i < pageSections.length; i++) {
		if (pageSections[i] == sectionId ) {
			document.getElementById(pageSections[i]).style.visibility = "visible";
		} else {
			$("#" + pageSections[i].id).hide();
		} 
    }*/
}
